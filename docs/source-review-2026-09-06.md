> [!NOTE]
> Drafted by a LLM-based AI tool (Codex/GPT-6).

<!-- cspell:words HSGP MCMC BFMI logit elpd lengthscale nonzero subfit -->

# Source review, 6 September 2026

The review found errors in statistical calculations, diagnostic decisions, plotting, report lookups and upload results. The accompanying fixes include regression tests. The starting commit was `8ee0c8c4e1b8cd68a00d0d163a1c2b79a32d7c52`. The PR was subsequently rebased onto `7b27e8e` and updated in response to reviewer comments.

The review covered the Python library, its tests, the Markdown formatting script, and the build and CI configuration. The Python source contains about 5,600 lines. The .NET area has no implementation to review. The checks below concern the shared library; they do not establish the validity of models fitted by consuming repositories.

## Findings and fixes

Priority 1 denotes a defect that can change model calculations or make an unreliable result appear acceptable. Priority 2 denotes an incorrect output or a failure for a supported input.

HSGP means Hilbert space Gaussian process, an approximation built from a finite set of basis functions. MCMC means Markov chain Monte Carlo, the sampling method used to estimate posterior distributions. BFMI is an energy diagnostic for those chains. LOO means leave-one-out prediction, and ESS means effective sample size, an estimate of how much information correlated draws provide. ROPE means region of practical equivalence, the range of effects treated as small enough to be practically negligible.

| Priority | Area                    | Defect and correction                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1        | HSGP approximation      | The boundary used the largest absolute input rather than half the input range, although PyMC centres the inputs at their midpoint. An offset could therefore change the approximation to the same covariance function. Increasing the boundary floor also left the basis size unchanged. The helper now uses the centred range and increases the basis size with the boundary. It rejects malformed, constant and non-finite inputs before constructing the model. |
| 1        | MCMC energy diagnostic  | BFMI treated the first stored axis as chains. Transposing the named `chain` and `draw` dimensions made it compare chains rather than successive draws. It now reads the dimensions by name.                                                                                                                                                                                                                                                                        |
| 1        | Convergence decisions   | A summary missing either bulk or tail effective sample size could pass the convergence gate. The gate now requires all three diagnostic columns and at least one summary row. A shared helper keeps this rule consistent with sampling-quality extraction.                                                                                                                                                                                                         |
| 1        | LOO reliability         | NaN diagnostic values and non-finite thresholds could be labelled reliable. Reliability now requires a finite threshold and finite diagnostic values. A valid threshold of zero is preserved in the summary table.                                                                                                                                                                                                                                                 |
| 1        | LOO relative efficiency | The fallback caught every `LookupError`, including the `KeyError` raised when a requested parameter was missing. It silently changed the calculation and printed an inaccurate explanation. The fallback now applies only when no parameter names were supplied or recorded. Missing named parameters and empty selections raise an error.                                                                                                                         |
| 2        | Diagnostic reports      | All-missing extrema produced bare `NaN` tokens in JSON. The writer now emits `null`, and the report shows an unavailable value. Diagnostic tables now flag missing and non-finite cells, including pandas missing values that previously caused rendering to fail. Sampling-quality extraction retains an available ESS when the other ESS column is entirely missing.                                                                                             |
| 2        | Statistical summaries   | Interval bounds discarded infinite draws but the median retained them. All parts of the summary now use finite draws. Ordinary least squares now rejects a constant predictor, whose slope cannot be identified separately from the intercept, and rejects non-finite observations. ROPE summaries reject non-finite half-widths. The inverse logit uses SciPy's stable sigmoid to avoid overflow for large negative inputs.                                       |
| 2        | ArviZ compatibility     | The documented compatibility helpers failed when legacy `groups` was a method or LOO results used `elpd_loo` and `p_loo`. Both conventions now work.                                                                                                                                                                                                                                                                                                               |
| 2        | Plotting                | NumPy GP sample arrays raised an ambiguous-truth-value error. Single-sample prior draws lost their sample axis. Rectangular contour grids were reshaped as squares. An explicit line drawing order and a custom graph layout caused duplicate or unsupported arguments. Each path now accepts the corresponding input.                                                                                                                                             |
| 2        | Figure lifetime         | Saving one plot collection closed every open matplotlib figure, including unrelated work. It now closes only figures owned by that collection.                                                                                                                                                                                                                                                                                                                     |
| 2        | Report and table output | Nearest-row lookup failed for empty tables or tables with no finite lookup keys. It now returns `None` and skips unusable keys. Truncated tables retain original ranks, so rows 99 and 100 are no longer relabelled as rows 3 and 4. A zero-row limit no longer displays the entire table.                                                                                                                                                                         |
| 2        | Azure upload results    | Returned URLs did not encode spaces, percent signs or fragment characters in blob names. The selected report URL could point to a nested `index.html` rather than the root report. Both are corrected. Rejection messages also stop copying a supplied SAS query string, which can contain credentials, into logs.                                                                                                                                                 |

The review also corrected two explanations. A single highest-density interval can span low-density gaps in a distribution with several modes. It is not a disjoint highest-density region. A median on an items scale need not be a transform of the native-scale median when the items-scale effect also depends on other parameters.

## Evidence and validation

The original suite passed all 386 tests. The review added 52 cases. Before the fixes, 49 new cases failed and three passed. The full suite then passed all 438 tests. This includes comparisons with known regression lines, exact covariance matrices, named xarray dimensions, rendered table cells and URL round trips. Azure tests use a simulated client and do not publish files.

For the HSGP covariance check, the inputs have 31 equally spaced points, the squared-exponential lengthscale is 0.2, and the calibration range is 0.2 to 0.3. Moving the input range from `[-1, 1]` to `[2, 4]` gave a maximum absolute covariance error of about 0.600 before the fix and 0.007 after it. With inputs in `[-1, 1]` and a boundary floor of 8, the error fell from about 0.694 to 0.0060 after the basis-rounding correction described below. The test compares the full approximate matrix with the exact matrix, including its unit diagonal. PyMC documents both midpoint centring and the relationship between boundary size and basis size in its [HSGP implementation](https://www.pymc.io/projects/docs/en/stable/_modules/pymc/gp/hsgp_approx.html).

The BFMI calculation retains the existing formula. The fix concerns which observations it compares. This agrees with ArviZ's use of an explicit `chain, draw` ordering in its [diagnostic implementation](https://python.arviz.org/en/v0.22.0/_modules/arviz/stats/diagnostics.html).

Ruff lint and formatting checks pass. Markdown formatting and spelling checks pass. The source distribution and wheel build successfully. The remaining test warnings come from existing tests that deliberately supply constant posterior parameters to ArviZ.

## Reviewer follow-up

The reviewer identified consumer assumptions that the initial library tests did not exercise. The follow-up adds explicit HSGP `L` and `center` arguments so a subset fit can preserve the full basis. Freezing only the half-width would still let PyMC change the midpoint. Tests compare functions with identical nonzero weights on full, reduced and one-row inputs. Boundary-floor calibration now applies PyMC's formula before integer truncation, giving 70 basis functions in the wide-boundary example instead of 67.

The changes also accept NumPy BFMI arrays, share diagnostic reductions, distinguish a completed scan from a failed one, sanitise nested JSON amendments, reject non-finite LOO thresholds, expose unavailable-parameter errors publicly, preserve literal labels in console messages, coerce report lookup keys, and retain raw upload paths alongside encoded URLs. A real ArviZ plot collection now exercises the public figure accessor. Tests use Rich's public column cells and cover an absent ESS column separately from an all-missing column. The inverse-logit docstring now states its NumPy input contract.

The follow-up adds 45 test cases, bringing the full passing suite to 483. The initial focused reproduction run failed 31 cases before the changes. The language-reading-predictors test `test_subfit_convergence_flags_low_bfmi` now passes with this branch's source in that project's environment. A check using vocabulary-growth's actual `unpublished_assets` function confirms that raw uploaded paths match filenames containing spaces, plus signs and non-ASCII characters; its current URL-stripping approach still needs migration. The dependency lock remains valid, and the built wheel and source distribution carry version 0.13.0.

The [0.13.0 migration guide](migrating-to-0.13.md) records the changed contracts and pending consumer work. These targeted checks do not establish that the consumers can adopt this version unchanged.

## Effect on existing work

The HSGP correction can change the prior approximation and the number of sampled basis weights. Existing fits that use these helpers need a fresh fit before their results can be compared with the corrected implementation. The covariance checks establish the specific numerical corrections above; they do not guarantee approximation accuracy for every input range or lengthscale prior.

The stricter diagnostic checks can change a previous pass into a review result when required information is absent. Invalid regression inputs and ROPE half-widths now raise explicit errors. No dependent research models were refitted, no Azure service was contacted, and no Windows runtime was exercised. The review does not claim that passing these tests proves the absence of other defects.
